import os
import sys

# Add the directory containing STEPP to the Python path
ROOT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.append(ROOT_DIR)
"""Absolute path to the STEPP repository."""